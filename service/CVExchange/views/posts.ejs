<!DOCTYPE html>
<html>
  <head>
    <title>CVExchange</title>
  </head>
  <body>
    <h1>CVExchange Where we exchange the V's and E's</h1>
    <% for (let post of posts) { %>
      <div class="post">
        <a href="./posts/<%= post.id %>"><%= post.title %></a>
        <p><%= post.text %></p>
      </div>
    <% } %>
  </body>
</html>